import React from "react";

import "../styles/InfoCardData.sass";

const Infocarddata = ({ children }) => {
  return <div className="info-card-data">{children}</div>;
};

export default Infocarddata;
